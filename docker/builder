FROM golang:alpine AS builder

RUN mkdir /build
ADD https://github.com/ArchiMoebius/hexer.git /build/
#ADD . /build/
WORKDIR /build

RUN apk add --no-cache sqlite make git alpine-sdk libx11-dev
RUN make linux
